[project]
name = "seo-workbench"
version = "0.2.0"
description = "Agent-neutral SEO workflow and audit tools."
requires-python = ">=3.11,<3.12"
dependencies = [
    "filelock>=3.18.0,<4",
    "matplotlib>=3.11.1",
]

[project.scripts]
seo-workbench = "seo_workbench.cli:main"

[build-system]
requires = ["hatchling>=1.27.0,<2"]
build-backend = "hatchling.build"

[project.optional-dependencies]
rendered = [
    "playwright>=1.53.0",
]
technology = [
    "wappalyzer==2.0.2",
]
google = [
    "google-auth>=2.40.0,<3",
    "google-auth-oauthlib>=1.2.2,<2",
]
ui = [
    "fastapi>=0.128.0,<1",
    "uvicorn>=0.35.0,<1",
    "watchfiles>=1.1.0,<2",
]

[dependency-groups]
dev = [
    "httpx2>=2.7.0,<3",
    "pytest>=8.4.0,<9",
    "pyyaml>=6.0.2,<7",
]

[tool.uv]
package = true

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.hatch.build.targets.wheel]
packages = ["seo_workbench", "seo_workbench_tools"]

[tool.hatch.build.targets.sdist]
include = [
    "seo_workbench/",
    "seo_workbench_tools/",
    "skills/",
    "templates/",
    "workflows/",
    "AGENTS.md",
    "README.md",
]
